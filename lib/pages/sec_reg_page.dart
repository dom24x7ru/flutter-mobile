import 'package:flutter/material.dart';

class SecRegPage extends StatelessWidget {
  const SecRegPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Страница регистрации'),
    );
  }
}